export const metadata = {title: 'Каталог - WebSofa'};

export default async function ProductPage() {
    return(
        <section>
<h1 style={{fontSize: 28, fontWeight: 800, marginBottom: 12}}>Каталог</h1>
<p className="muted">Здесь будет список товаров с ценой...</p>
        </section>
    );
}